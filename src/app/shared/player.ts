export class Player {

  // Add a booleon to show if the player is playing or not (has a name),
  // then this can be checked to get name, and Reset will switch it back.
  constructor(public name: string, public score: number, public hasName: boolean) {}



}
